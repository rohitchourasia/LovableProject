apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: lovable-previews
spec:
  type: ExternalName
  externalName: host.docker.internal
  ports:
    - name: api
      port: 9000
      targetPort: 9000
      protocol: TCP